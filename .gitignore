# Environment / secrets — never commit
.env
.env.local
.env*.local
*.env
frontend/.env.local
frontend/.env*.local

# Python (legacy — no longer used)
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Node
node_modules/
frontend/node_modules/
frontend/.next/
frontend/out/

# Supabase local dev
supabase/.branches/
supabase/.temp/

# OS
.DS_Store

# Ingest pipeline — raw scrapes and embeddings are heavy and regeneratable;
# only the pre-embedding chunks/*.jsonl files are committed as source of truth.
scraped/raw/
scraped/chunks/*.embedded.jsonl
ingest/.venv/
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
